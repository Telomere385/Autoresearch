Improve the Flappy Bird Q-learning evaluation mean_score within a small experiment budget.
Keep the Q-table state space unchanged, use real train/eval runs, and record mean_reward as a diagnostic metric when short runs still have zero score.
